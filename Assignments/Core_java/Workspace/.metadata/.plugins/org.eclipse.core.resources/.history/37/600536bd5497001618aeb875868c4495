package com.filehandling;

import java.io.*;
import java.util.ArrayList;
import java.util.Enumeration;

public class FileHandling4 {

	public static void main(String[] args) {
		readFiles();
	}

	private static void readFiles() throws FileNotFoundException {
		ArrayList<FileInputStream> fis = new ArrayList<>();
		fis.add(new FileInputStream(new File("abc.txt")));
		fis.add(new FileInputStream(new File("pqr.txt")));
		fis.add(new FileInputStream(new File("account.txt")));
		SequenceInputStream sis = new SequenceInputStrea;
	}

}
